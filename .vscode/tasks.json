{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "fix format",
            "type": "shell",
            "group": "build",
            "command": "poetry run black flatpak_builder_lint && poetry run isort flatpak_builder_lint",
        },
    ]
}